









CozyBlanket

Puzzle pieces have small hints for where to look for the next one
Inventory?
object triggers which tell pieces of story (subtle hints for puzzle pieces)

create emptys in puzzle spots, piece snaps to it with approximation, turn to true - when all true = game complete or smth

ray cast = collide w floor, approzimation rnage, above the empty= snap

//public float height = 2;
    //public GameObject pivot;

    //Vector3 direction = Vector3.down;
        //Ray theRay = new Ray(transform.position, transform.TransformDirection(direction * height));
        //Debug.DrawRay(transform.position, transform.TransformDirection(direction * height));


        
                //if (Physics.Raycast(theRay, out RaycastHit hit, 3f))
                //{
                //    if (hit.collider.gameObject == pivot)
                //    {
                //        selectedObject.transform.position = new Vector3(pivot.transform.position.x, pivot.transform.position.y, pivot.transform.position.z);
                //    }
                //}

    fix lock cam and remove reticle - when moving cam away the panel does not go away but the pieces count happens without destroying